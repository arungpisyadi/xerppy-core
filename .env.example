# Xerppy ERP Framework Environment Configuration
# Copy this file to .env and update the values as needed

# Flask Configuration
FLASK_APP=core.app:app
FLASK_ENV=development
SECRET_KEY=your-secret-key-change-in-production

# =============================================================================
# Database Configuration
# =============================================================================
# DB_TYPE Options:
#   - local  : Use local MySQL container (requires docker-compose --profile local-db)
#   - remote : Use remote database (AWS RDS, cloud DBs) - requires REMOTE_DB_URL
#   - supabase: Use Supabase PostgreSQL - requires SUPABASE_DB_URL
#   - sqlite : Use local SQLite database (default for development)
#   - mysql  : Use local MySQL without Docker
#   - postgres: Use local PostgreSQL without Docker
# =============================================================================
DB_TYPE=local

# Local Database Configuration (used when DB_TYPE=local or mysql)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=xerppy
DB_USER=root
DB_PASSWORD=your-db-password

# Remote Database Configuration (used when DB_TYPE=remote)
# Full connection string for remote database (AWS RDS, Supabase, cloud DBs, etc.)
# Examples:
#   MySQL:  mysql+pymysql://user:password@hostname:3306/database
#   Postgres: postgresql://user:password@hostname:5432/database
REMOTE_DB_URL=

# Supabase Configuration (use when DB_TYPE=supabase)
SUPABASE_DB_URL=postgresql://postgres:your-password@host:5432/postgres

# SQLite Configuration (use when DB_TYPE=sqlite)
SQLITE_PATH=xerppy.db

# LDAP Configuration (optional)
LDAP_ENABLED=false
LDAP_HOST=ldap://localhost
LDAP_PORT=389
LDAP_BASE_DN=dc=example,dc=com
LDAP_USE_SSL=false
LDAP_USE_TLS=false
LDAP_USER_DN=uid={username},ou=users,{base}

# Google SSO Configuration (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_REDIRECT_URI=http://localhost:8003/auth/google/callback

# CrewAI Configuration (optional)
# NOTE: CREWAI_API_KEY is your OpenAI API key - get it from https://platform.openai.com/api-keys
# CrewAI uses OpenAI's GPT models for agent reasoning
CREWAI_API_KEY=your-openai-api-key

# Alternative: Use OPENAI_API_KEY if you already have it set elsewhere
# OPENAI_API_KEY=your-openai-api-key

# Upload Configuration
UPLOAD_FOLDER=uploads
